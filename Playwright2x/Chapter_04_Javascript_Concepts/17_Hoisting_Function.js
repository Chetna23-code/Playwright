function getUserStatus() {
    //var status_code, JS Engine (optimized the code)
    console.log(status_code);
    var status_code = "Active";
    console.log(status_code);
}


getStatusCode();

//Note: var is function-scoped, so status is hoisted to
//the top of get getUserStatus(), not the global scope.


// getStatusCode();  if we call the function before defining this will also work in JS
//function getUserStatus() {
//var status_code, JS Engine (optimized the code)
// console.log(status_code);
//     var status_code = "Active";
//     console.log(status_code);
// }


